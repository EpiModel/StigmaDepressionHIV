# Scratchpad for interactive testing before integration in a script

rgc.prob,0.2662722,numeric, CPN-plogis(qlogis(0.25546) + log(1.25)) #0.357;  # gaps appendix 9.4
ugc.prob,0.225,numeric,CPN-gaps appendix 9.4 | calibrated
rct.prob,0.1923077,numeric, CPN-plogis(qlogis(1714429) + log(1.25)) #0.3216; # gaps appendix 9.3
uct.prob,0.16,numeric, CPN-gaps appendix 9.3 | calibrated

plogis(qlogis(0.16) + log(1.25))
